{
  "name": "@monotodo/database",
  "version": "0.0.0",
  "scripts": {
    "db:generate": "dotenvx run -- prisma generate",
    "db:migrate": "dotenvx run -- prisma migrate dev",
    "db:seed": "dotenvx run -- prisma db seed",
    "db:reset": "dotenvx run -- prisma migrate reset",
    "db:studio": "dotenvx run -- prisma studio"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^6.18.0",
    "@prisma/client": "^6.18.0"
  },
  "devDependencies": {
    "@monotodo/typescript-config": "workspace:*",
    "@types/node": "^24.9.1",
    "prisma": "^6.18.0",
    "tsx": "^4.20.6"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
